# GameMaker-Macros

My GameMaker macros

Since the .yy file which contains metadata is in the .gitignore (as the settings are different per game), it must be manually added to each game this submodule is on through GameMaker's "Add Existing"
