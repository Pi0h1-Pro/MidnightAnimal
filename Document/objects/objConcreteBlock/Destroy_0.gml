/// @description  Set automatic tiling.
scrAutoTile();

if (up)
	with (up)
		scrAutoTile();
if (down)
	with (down)
		scrAutoTile();
if (left)
	with (left)
		scrAutoTile();
if (right)
	with (right)
		scrAutoTile();

