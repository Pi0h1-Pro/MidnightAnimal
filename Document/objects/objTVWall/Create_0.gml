on = choose(0, 1);
if (on == 1)
{
	image_speed = 0.15;
	
	// my_id = instance_create(x, y, objLightTV)
	// my_id.image_angle = image_angle
}
else
	image_speed = 0;
image_index = random(1);
life = 20;
energie = 3;

