image_angle = random(360);
friction = 0.15 + random(0.1);
diradd = -1 + round(random(1)) * 2;
maxspeed = 1;
life = 3 + round(random(4));
image_index = floor(random(sprite_get_number(sprite_index)));
dist = 6 + random(22);
image_speed = 0;

// image_speed=0.1+random(0.1)
wait = 60 + random(40);

