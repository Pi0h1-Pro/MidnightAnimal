/// @description Draw papers.

// Adjust the last two arguments to adjust the height of the papers; speed * rotspeed for example is greater than speed * rotspeed / 2.
draw_sprite_skew_ext(sprite_index, image_index, x, y, image_xscale, image_yscale, image_angle, c_white, 0.9, speed * rotspeed, speed * rotspeed)


